import { DatenumberPipe } from './datenumber.pipe';

describe('DatenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DatenumberPipe();
    expect(pipe).toBeTruthy();
  });
});
